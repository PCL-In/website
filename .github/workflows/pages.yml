name: PCL-In Download Site (GitHub Pages)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write
  actions: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Delete stale github-pages artifacts
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          ids=$(gh api "repos/${GH_REPO}/actions/artifacts?per_page=100" --paginate --jq '.artifacts[] | select(.name == "github-pages") | .id' || true)
          for id in $ids; do
            echo "Deleting stale github-pages artifact: $id"
            gh api -X DELETE "repos/${GH_REPO}/actions/artifacts/${id}" >/dev/null 2>&1 || true
          done

      - name: Stage site files
        run: |
          mkdir -p _site
          rsync -a --exclude '.git' --exclude '.github' --exclude '_site' ./ _site/
          ls -la _site

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          name: github-pages
          path: _site

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4

      - name: Submit to IndexNow (Bing / Yandex)
        run: |
          KEY="0f2d4ec4e15449cca2e7226fc510ca97"
          code=$(curl -sS -m 25 -o /dev/null -w "%{http_code}" \
            -X POST "https://api.indexnow.org/indexnow" \
            -H "Content-Type: application/json; charset=utf-8" \
            -d "{\"host\":\"pclin.astras.cc\",\"key\":\"${KEY}\",\"keyLocation\":\"https://pclin.astras.cc/${KEY}.txt\",\"urlList\":[\"https://pclin.astras.cc/\"]}" || true)
          echo "IndexNow HTTP ${code} (200/202 = accepted; failure is ignored)"

      - name: Clean up old github-pages artifacts
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          ids=$(gh api "repos/${GH_REPO}/actions/artifacts?per_page=100" --paginate --jq '.artifacts[] | select(.name == "github-pages") | .id' || true)
          # Keep only the most recent one (the one we just deployed).
          first=1
          for id in $ids; do
            if [ "$first" = "1" ]; then first=0; continue; fi
            echo "Pruning older github-pages artifact: $id"
            gh api -X DELETE "repos/${GH_REPO}/actions/artifacts/${id}" >/dev/null 2>&1 || true
          done
